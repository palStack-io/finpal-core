# Per-run scratch. `captured/` is regenerated by the capture on every run and is
# derived from the components, so a committed copy would be measured as if it were
# today's code — the staleness `capture-modals.walk.tsx` clears the directory to
# avoid. The Chrome profile is per-pid because a live Chrome LOCKS it.
captured/
.pages/
.chrome-profile*/
