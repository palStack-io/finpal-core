flask==2.2.5 
flask-sqlalchemy==2.5.1 
flask-login==0.6.3 
flask-migrate==4.1.0 
flask-mail==0.10.0 
python-dotenv==1.0.1 
gunicorn==23.0.0 
sqlalchemy==1.4.54 
werkzeug==2.2.3 
psycopg2-binary==2.9.9 
requests>=2.31  
# OIDC Authentication
oauthlib==3.2.2 
requests-oauthlib==1.3.1 
PyJWT==2.8.0 
cryptography==41.0.5 
pyOpenSSL==23.2.0 
flask-apscheduler
pytz>=2021.1
tzdata>=2021.1
Flask-Migrate>=3.1.0
Flask-SQLAlchemy>=2.5.1
alembic>=1.7.5

psycopg2-binary>=2.9.1  # For PostgreSQL

# REST API Dependencies
flask-jwt-extended==4.6.0  # JWT authentication for API
flask-cors==4.0.0  # CORS support for React Native
marshmallow==3.21.0  # Serialization/deserialization
marshmallow-sqlalchemy==1.0.0  # SQLAlchemy integration
flask-restx==1.3.0  # REST API with Swagger documentation
flask-limiter==3.5.0  # Rate limiting for auth endpoints

# Investment integrations
# These two were pinned this low to stay Python 3.8 compatible. That constraint
# is gone — the image is python:3.12-slim now — so the pins are only conservatism.
# yfinance is at 1.5.x; we are on 0.2.18, roughly two years and a major version
# behind, which matters because old yfinance breaks as Yahoo changes its API.
# Upgrading needs the three call sites in integrations/investments/yfinance.py
# (yf.Ticker, .info, .history) checked against live data, so it is its own change.
multitasking==0.0.11
yfinance==0.2.18

# pointsPal fuzzy matching
rapidfuzz>=3.6.0

# 29a41de6a866d56c36aba5159f45257c
