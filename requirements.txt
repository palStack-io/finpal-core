flask==3.1.2
# 2.5.1 -> 3.0.5 is NOT optional here and is not a separate upgrade: 2.5.1 does
# `from flask import _app_ctx_stack` at module import, and Flask REMOVED that in
# 2.3. So Flask 3 breaks Flask-SQLAlchemy 2.5.1 before a single line of app code
# runs. They move together or not at all.
# 3.0.5 was the stepping stone while SQLAlchemy stayed on 1.4; 3.1 requires
# SQLAlchemy >= 2.0.16 and is correct now that it is on 2.0.
flask-sqlalchemy==3.1.1
flask-login==0.6.3 
flask-migrate==4.1.0 
flask-mail==0.10.0 
python-dotenv==1.0.1 
gunicorn==23.0.0 
# 1.4.54 -> 2.0. NO application code changed for this: the roadmap recorded
# "64 Query.get() calls, REMOVED in SQLAlchemy 2", and that is not so —
# Query.get() is LEGACY in 2.0 (it warns), not removed. Verified against real
# PostgreSQL 15, not just SQLite. The 65 call sites still work and are tracked
# by tests/test_legacy_orm_usage_only_shrinks.py so the debt cannot grow.
sqlalchemy==2.0.36
werkzeug==3.1.3
psycopg2-binary==2.9.9 
requests>=2.31  
# OIDC Authentication
oauthlib==3.2.2 
requests-oauthlib==1.3.1 
PyJWT==2.8.0 
cryptography==41.0.5 
pyOpenSSL==23.2.0 
flask-apscheduler
pytz>=2021.1
tzdata>=2021.1
# Flask-Migrate and Flask-SQLAlchemy were ALSO listed here as >= ranges, three
# lines apart from their == pins above, under different capitalisation. Both
# resolved to the pin, so nothing was broken - but during an upgrade a second
# looser line for the same package is exactly the thing that makes a resolution
# hard to read. Removed; the pins above are the single source.
alembic>=1.7.5

# psycopg2-binary is pinned to 2.9.9 above; the duplicate >=2.9.1 line that sat
# here said the same thing more weakly.

# REST API Dependencies
flask-jwt-extended==4.6.0  # JWT authentication for API
flask-cors==4.0.0  # CORS support for React Native
marshmallow==3.21.0  # Serialization/deserialization
marshmallow-sqlalchemy==1.0.0  # SQLAlchemy integration
flask-restx==1.3.0  # REST API with Swagger documentation
flask-limiter==3.5.0  # Rate limiting for auth endpoints

# Investment integrations
# yfinance was pinned to 0.2.18 to stay Python 3.8 compatible. That constraint went
# away with the move to python:3.12-slim, and the old pin was not merely stale — it
# was broken. Verified against live Yahoo from one IP, seconds apart:
#
#   0.2.18  ticker.info -> HTTPError 429 Too Many Requests
#   1.5.2   ticker.info -> 497.27
#
# Old yfinance does not negotiate Yahoo's cookie/crumb, so every quote is rejected
# and investment price refresh silently fell back or failed.
#
# We use exactly three things — yf.Ticker(), .info and .history(period=) — all
# confirmed working on 1.5.2, and no code touches pandas or numpy directly.
# multitasking is no longer pinned here: it is a yfinance dependency and only had
# an explicit pin because 0.0.12+ needed Python 3.9+.
yfinance==1.5.2

# pointsPal fuzzy matching
rapidfuzz>=3.6.0
