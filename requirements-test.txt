pytest==8.3.4
pytest-flask==1.3.0
# Ranged, not pinned: 6.x requires Python 3.9+, but the production image is
# built on ubuntu:20.04 and runs 3.8. An exact 6.0.0 pin cannot install against
# the Python that actually ships.
pytest-cov>=5,<7
factory-boy==3.3.1
